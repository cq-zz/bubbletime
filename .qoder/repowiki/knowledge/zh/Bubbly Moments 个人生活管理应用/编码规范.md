- 服务层函数统一返回包含 code、message、data 字段的标准响应对象，通过 wrapSuccess/wrapFail 包装。
- 数据访问层对输入参数进行标准化处理（normalizeDbValue/normalizeSqlValue），确保类型安全与空值兼容。
- UI 样式高度依赖 src/utils/theme.js 中定义的 colors、spacing、radius 等设计令牌，保持视觉一致性。